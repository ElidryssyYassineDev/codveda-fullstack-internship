// client/src/pages/ProductsPage.jsx
// Zero changes to ProductList itself — this just gives your existing,
// fully-working CRUD + live socket sync a real URL to live at.
import ProductList from '../components/ProductList'

function ProductsPage() {
  return <ProductList />
}

export default ProductsPage